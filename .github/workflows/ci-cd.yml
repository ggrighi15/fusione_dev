# DEPRECATED: use .github/workflows/ci.yml
name: deprecated-ci-cd

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: echo "This workflow is deprecated. Use FusionCore CI (.github/workflows/ci.yml)."
